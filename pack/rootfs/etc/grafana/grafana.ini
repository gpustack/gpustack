# Managed by GPUStack. Most settings are supplied via GF_* env vars.
[server]
root_url = %(protocol)s://%(domain)s:%(http_port)s/grafana
serve_from_sub_path = true

[auth.anonymous]
enabled = true
org_role = Viewer

[auth]
disable_login_form = true
